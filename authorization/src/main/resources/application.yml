server:
  port: 7080

spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/authorization_db
    username: myuser
    password: User@123
    driver-class-name: com.mysql.jdbc.Driver